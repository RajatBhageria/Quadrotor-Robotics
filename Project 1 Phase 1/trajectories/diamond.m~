function [desired_state] = diamond(t, qn)
% DIAMOND trajectory generator for a diamond

% =================== Your code goes here ===================
% You have to set the pos, vel, acc, yaw and yawdot variables
% NOTE: the simulator will spawn the robot to be at the
%       position you return for t == 0
 
maxT = 14; 
cs = [0;0;0.2449;-0.0466];
theta = dot(cs, [1, t, t^2, t^3]'); 
omega = dot(cs, [0, 1, 2*t, 3*t^2]'); 
alpha = dot(cs, [0, 0, 2, 6*t]'); 
 
v1 = [1/4; sqrt(2); sqrt(2)];
v2 = [1/4; -sqrt(2); sqrt(2)];
v3 = [1/4; -sqrt(2); -sqrt(2)];
v4 = [1/4; sqrt(2); -sqrt(2)];
 
pos = [];
vel = [];
acc = [];
 
if (t==0)
    pos=[0;0;0];
    vel=[0;0;0];
    acc=[0;0;0];
    
elseif (t<.25*maxT)
    pos=v1*theta+[0;0;0];
    vel=v1*omega; 
    acc=v1*alpha;
    
elseif (t<.50*maxT) && (t >= .25*maxT)
    pos=v2*theta+[1/4;sqrt(2);sqrt(2)];
    vel=v2*omega; 
    acc=v2*alpha;
    
elseif (t<.75*maxT) && (t >= .50*maxT)
    pos=v3*theta+[1/2;0;2*sqrt(2)];
    vel=v3*omega; 
    acc=v3*alpha;
    
elseif (t<maxT) && (t >= .75*maxT)
    pos=v4*theta+[3/4;-1*sqrt(2);sqrt(2)];
    vel=v4*omega; 
    acc=v4*alpha;
    
else
    pos=[1;0;0];
    vel=[0;0;0]; 
    pos=[0;0;0];
end



yaw = 0;
yawdot = 0;

% =================== Your code ends here ===================

desired_state.pos = pos(:);
desired_state.vel = vel(:);
desired_state.acc = acc(:);
desired_state.yaw = yaw;
desired_state.yawdot = yawdot;

end

function theta 
